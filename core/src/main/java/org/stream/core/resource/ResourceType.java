package org.stream.core.resource;

/**
 * The resourceType currently stream supports, to support other objects, please feel free to extend this enum.
 * @author guanxwei
 *
 */
public enum ResourceType {
   PRIMITIVE, OBJECT, MODELMAP, SESSION, HTTPREQUEST;
}
